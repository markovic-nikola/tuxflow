# TuxFlow

Linux-only desktop app for managing dev environments — processes, AI coding agents, and terminals from one window. Inspired by [SoloTerm](https://soloterm.com/) but native GTK4 for Linux.

## Stack

- **Language:** Rust
- **GUI:** GTK4 + libadwaita (gtk4-rs, vte4-rs)
- **Terminal:** VTE4 (vte-2.91-gtk4)
- **Async:** tokio
- **Config:** TOML (serde + toml crate)
- **MCP:** rmcp (Rust MCP SDK, Unix socket transport)
- **File watching:** notify v7

## Build & Run

```bash
# System deps (Ubuntu 24.04)
sudo apt install libgtk-4-dev libadwaita-1-dev libvte-2.91-gtk4-dev build-essential

# Build & run
cargo build
cargo run
cargo run -- /path/to/project
```

## Project Structure

Cargo workspace since migration step M0 (2026-08-26; branch `iced-migration`): the GNOME-stack exit is underway. `tuxflow-core` is the GUI-independent heart shared by both shells; the GTK app (`tuxflow`, root package) keeps working unchanged throughout — its modules re-export core (`pub use tuxflow_core::...`), so `crate::` paths inside it still resolve. `tuxflow-iced` is the migration target, built on the vendored iced_term fork (see `vendor/iced_term/VENDOR.md` for the patch-by-patch history; the spike in `spike/iced-term-spike/` is the frozen de-risking record with its own lockfile, consuming the same vendor).

```
tuxflow-core/              # GUI-independent (no GTK/iced/VTE types allowed)
  src/
    config/
      schema.rs            # Serde structs for tuxflow.toml (ProcessConfig, ProcessCategory, etc.)
      loader.rs            # TOML loading, validation, defaults
      keybindings.rs       # Serde half only: ShortcutAction, KeybindingsSettings (strings)
      projects.rs          # Project config management (~/.config/tuxflow/projects.toml)
      settings.rs          # Global settings (~/.config/tuxflow/settings.toml)
      ssh.rs               # SSH config parser (~/.ssh/config host extraction)
    detect/
      detector.rs          # Tech stack auto-detection (package.json, Cargo.toml, etc.) over ProjectFs
    remote/
      mod.rs               # ProjectLocation (Local/Ssh), sh_quote, ssh ControlMaster options, remote command wrapping
      fs.rs                # ProjectFs trait: LocalFs (std::fs) / SshFs (ssh exec over shared connection)
      mic.rs               # MicBridgeManager: ssh -R socket + fake arecord, so remote agents get voice input
      ports.rs             # What a remote run actually listens on (tmux pane → process tree → ss)
      tunnel.rs            # TunnelManager: ssh -L port forwards for detected ports on remote projects
      vite.rs              # Reads laravel-vite-plugin's public/hot — fallback for hosts without tmux
    mcp/
      server.rs            # MCP server on Unix socket (/tmp/tuxflow-<project>.sock)
      tools.rs             # MCP tools: list_processes, get_process_logs, start/stop/restart
      bridge.rs            # UI<->MCP thread bridge, LogBuffer (VecDeque ring buffer)
    util/port_detector.rs  # Regex scan terminal output for ports/URLs
  tests/                   # config, detector, log buffer, port detector, projects roundtrip, remote fs
  examples/                # detector_check, port_scan_check, remote_ports_check
tuxflow-iced/              # The iced shell — feature-complete for remote work; TESTING.md = walkthrough guide
vendor/iced_term/          # Hardened iced_term fork (MIT) — VENDOR.md documents every patch
spike/iced-term-spike/     # Frozen spike record (own workspace; README = VTE-parity scorecard)
src/                       # The GTK app — package `tuxflow` (root)
  main.rs                  # Entry point
  app.rs                   # GtkApplication subclass
  workspace.rs             # Multi-project workspace management
  bin/tuxflow-mcp.rs       # Standalone MCP server binary
  config/
    keybindings.rs         # gdk runtime half (Keybinding, KeybindingMap); serde half re-exported from core
  process/
    manager.rs             # ProcessManager: spawn/kill/restart via VTE
    auto_restart.rs        # Crash detection + exponential backoff
    pid_file.rs            # PID file tracking
  watcher/
    file_watcher.rs        # notify + glob matching, triggers process restart
  ui/
    window.rs              # Main AdwApplicationWindow — central wiring hub
    accent.rs              # Accent theming: app accent + local/remote sidebar accents
    terminal_theme.rs      # Terminal color schemes
    terminal_view.rs       # VTE terminal wrapper
    terminal_search.rs     # Ctrl+F search overlay
    command_palette.rs     # Command palette (Ctrl+Shift+P)
    composer_bar.rs        # Local message composer under agent terminals (beats ssh typing lag)
    add_command_dialog.rs  # Add command/agent dialog
    add_remote_project_dialog.rs # Add remote (SSH) project dialog with BatchMode verification
    add_ssh_dialog.rs      # Add SSH connection dialog
    edit_project_dialog.rs # Edit project dialog
    project_detail.rs      # Project overview panel
    status_bar.rs          # Bottom bar: actions + git state + remote indicator
    sidebar/
      project_list.rs      # Sidebar with expandable project sections
      project_row.rs       # Project row (icon, name, controls)
      process_row.rs       # Process row (status dot, name, port)
      section_header.rs    # Section header (AGENTS 5/5, COMMANDS 2/5, SSH 1/3)
      dnd.rs               # Drag-and-drop reordering
    settings/
      settings_window.rs   # AdwPreferencesWindow with all settings tabs
  util/
    worker.rs              # run(): blocking work on a thread, result on the GTK main loop
    icon_detector.rs       # Project icon auto-detection
    notifications.rs       # Desktop notifications via libnotify
data/
  style.css                # Application stylesheet
  icons/                   # SVG icons + hicolor hierarchy
  com.tuxflow.TuxFlow.desktop
  com.tuxflow.TuxFlow.metainfo.xml
```

## Architecture Notes

- **Process categories:** `ProcessCategory::Command`, `Agent`, `Terminal`, `SSH` — each gets a dedicated sidebar section. SSH connections are VTE terminals running `ssh` commands, reusing the full process management infrastructure (auto-restart handles reconnection)
- **MCP bridge:** `mcp/bridge.rs` uses `LazyLock<Arc<Mutex<>>>` globals for cross-thread state. `LogBuffer` is a 1000-line VecDeque ring buffer fed by VTE `contents-changed` signal
- **Settings persistence:** All settings save immediately to `~/.config/tuxflow/settings.toml` (24 save points in settings_window.rs)
- **Accents (ui/accent.rs):** one palette feeds three pickers — the app accent (`accent_bg_color`/`accent_fg_color`/`accent_color`) plus `local_accent` and `remote_accent`, the sidebar hues that say where a project lives (green / logo gold by default). The sidebar takes an entry's *text-weight* `accent`, never its button `bg`, and swaps to `accent_light` on light surfaces, since these are read as text and thin borders — that scheme-dependence is the whole reason the colors are generated in Rust at USER priority instead of living in style.css. style.css keeps only the dark values as a pre-`apply` fallback, and they must stay at the top of the file: GTK resolves `@color` references at parse time, so a use above the `@define-color` silently drops the declaration. Local rules are the base ones and `.project-remote` overrides follow them — remote wins over "running" on purpose. Unknown names (hand-edited settings) fall back to the shipped defaults rather than leaving the color undefined, which would drop every rule using it. The same generator carries `status_working`/`status_restarting`: fixed meanings, but both ambers measure ~2.2:1 on the light sidebar (below the 3:1 a dot owes) so they need a dark twin. Contrast is unit-tested against both schemes — 4:1 for the accents (read as text), 3:1 for the dots. The constants there are the *stricter* #ebebeb/#303030, not the #fafafa/#222226 the app actually paints, so the assertions keep a margin
- **URL handling:** VTE regex matching + Ctrl+click opens via `xdg-open`. Sidebar/status bar browser buttons use `gtk4::UriLauncher`
- **Port/URL detection (tuxflow-core/src/util/port_detector.rs):** badge priority is labeled "Preview URL:" (preferred, locks even though remote — `shopify app dev`'s admin URL beats its localhost GraphiQL) > local > provisional remote (OAuth links, upgradeable). Infra/error lines never badge ("proxy server started", "econnrefused", vite tool lines) but their ports still harvest into `seen_local` for tunnelling. Tool lines do have a *fallback* tier below `local`: a dev server's own address line (`➜  Local:  http://localhost:5173/`, matched by shape in `is_arrowed_label` — vite picks `➜` or `→` by font, so literal phrases made the badge depend on a glyph) badges when nothing else claims one, marked `tool: true` so `badge_final` stays false and the app's port still takes over when it prints. Without it a project whose API dies (bun on EADDRINUSE beside vite) shows no port, no URL and never auto-opens, since every consumer in window.rs hangs off the badge. Only *address* lines get the fallback, never the rest of the tool class — auto-opening `shopify app dev`'s proxy port is the bug that tier must not reintroduce. A stricter class (`FOREIGN_PORT_PHRASES`) is skipped *entirely*, harvesting included, because the port named belongs to someone else: vite's "Port 5173 is in use, trying another one" and — since `php artisan serve` walks `--tries` ports and echoes PHP's bind failure verbatim — "Failed to listen on 127.0.0.1:8000 (reason: Address already in use)". Both announce the retry and the port they retried onto, so dropping the line loses nothing. Match the bare phrase (`in use`), not each server's sentence around it — bun's "error: Failed to start server. Is port 3000 in use?" puts the port *inside* what used to be matched as "is in use", and bun then exits rather than retrying, so no later line corrects a badge that latched the neighbour's port. Getting this wrong is not a cosmetic off-by-one on a remote project: 8000 there is a *neighbouring project's* server, so the badge locks to it, tunnels to it (remapped, since local 8000 is already forwarded elsewhere) and the browser button silently opens the wrong app under this project's name. Match on the phrase, not the reason — the multiplex TUI clips the line to pane width, so "Address already…" is elided while "Failed to listen" survives on the same row as the port. Auto-open (one-shot per user start) fires when the badge is *final*, or after a 5 s grace for provisional-only badges — never on first-URL-seen. Ink-based CLIs (shopify) hard-wrap output at width−1 with a real newline, which even `tmux capture-pane -J` can't rejoin — remote scans go through `scan_output_wrapped`, which re-joins rows of width/width−1 before parsing (local VTE joins its own soft wraps natively). A row ending on a box-drawing glyph is exempt: full-screen TUIs (`php artisan dev` → `@laravel/multiplex`) border the pane, so *every* row measures full width and joining them fuses the whole screen into one logical line — where one "Failed to listen" swallows the port announced rows below and the badge comes back empty. Width alone cannot tell a frame edge from a wrap; Ink's wraps break mid-token and never land on a border. `tuxflow-core/examples/port_scan_check.rs` replays a `tmux capture-pane` dump through this path when a badge misbehaves. Custom commands in projects.toml override same-named detected processes at load (a user's edit of a detected process persists there)
- **`TUXFLOW_CHILD=1`** env var is set in window.rs and inherited by child processes (used to prevent recursive spawning)
- **Updates:** the GitHub check runs once per launch (15-min cache in `~/.cache/tuxflow/update-check.json`, storing the latest version unfiltered so the same cache re-evaluates correctly against a changed running version). The status-bar chip has two states behind a single click handler (`UpdateBadge` in status_bar.rs — re-connecting `clicked` per state would stack handlers and open two dialogs): "Update available" opens release notes + `pkexec apt-get install` of the .deb; "Restart to finish updating" goes straight to the restart prompt. The second state comes from a 30 s poll of `/proc/self/exe` — dpkg renames the new binary over ours, so the link gains a `" (deleted)"` suffix, and that is the *only* signal that the system's software manager upgraded us underneath a running window (the process keeps running the old code quite happily). Release builds only: `cargo run` replaces its own binary on every rebuild and would light the chip permanently. Restart hands off to a detached `sh` that waits for our PID to exit, since the app is single-instance and a second launch just activates the old window
- **Remote projects:** a project's `ProjectLocation` is `Local(PathBuf)` or `Ssh { host, dir }`, persisted in projects.toml as an opaque `ssh://host/path` key (no schema migration). Remote processes spawn as `exec ssh -t <mux> host '<wrap>'` in the normal VTE pipeline; all ssh invocations share a ControlMaster connection (`$XDG_RUNTIME_DIR/tuxflow/ssh-%C`). The wrap runs the command inside a tmux session on a dedicated server (`tmux -L tuxflow`, status off, mouse on, deterministic `tf-<proc>-<fnv32>` session names from project key + process name), so connection loss and app quit only *detach* — reconnect and app relaunch reattach via `new-session -A`; the command's exit code round-trips through `/tmp/.<session>-<uid>.exit` (uid-namespaced — deterministic session names would otherwise collide across users on a shared host) so crash detection still works. Hosts without tmux fall back inline to direct exec (no persistence; `pkill -s` on the pidfile PID covers the kill). Stop interrupts the tmux session (C-c) and waits up to ~2 s for it to end before killing it, then sweeps the pidfile login session, and flags the next spawn to kill-before-create (`remote_fresh_next` — avoids racing the fire-and-forget remote kill). The grace exists so a program gets to run its own cleanup: Vite removes the `public/hot` file naming its dev server, and an orphaned one leaves the app serving asset URLs pointing at a dead port long after the run that wrote it. Everything after the initial lookup targets tmux's unique *session id*, never the name — names are deterministic and reused, so a restart respawns the same name inside the grace window and a name-targeted wait would end by killing the session it just started. Agents treat C-c as "interrupt" rather than "quit" and are expected to fall through to the kill; app quit calls `detach_all()` instead, so remote processes deliberately outlive TuxFlow. Exit 255 on a remote process is surfaced as "connection lost — reconnecting", not a crash; reconnects retry forever (backoff capped at 32 s, one notification per outage) since the session is still alive on the host. Project load probes `tmux list-sessions` and auto-reattaches processes whose sessions are still alive, so the UI never shows "stopped" for a running detached process. Mouse selections inside tmux reach the local clipboard via TuxFlow's own bridge (selection gesture on a remote terminal → newest tmux paste buffer over ssh → GTK CLIPBOARD *and* PRIMARY, change-detected by hash) — NO released VTE implements OSC 52 (verified through 0.84), so the standard escape-sequence route is a dead end. The hard problem is not fetching the buffer but knowing whether it is *ours*: `show-buffer` always answers, and answers with the newest buffer on that server, which may be the drag that just finished or an OSC 52 an agent sent half an hour ago that nothing has displaced since. Publishing the second as if it were the first is what silently reverted the user's Ctrl+C to scrollback nobody had selected — it reads as "Ctrl+C sometimes doesn't work". Three gates decide, and dropping any one brings the bug back in a different disguise: the gesture must be able to *make* a selection (`SelectionGesture` in window.rs — a drag, or a double/triple click for tmux's word/line copy; firing on every button release meant a plain click to focus a pane republished a buffer from minutes earlier), the buffer must be younger than 5 s (`fetch_tmux_buffer` returns `#{buffer_created}` next to the host's own `date`, both read on the host so a skewed local clock can't fake freshness — the window is loose on purpose, since a false negative silently drops the user's copy while the race it guards needs a gesture that selected nothing within 5 s of a program's copy), and the hash must differ from the last publish. GTK4 dropped the multi-press event types and GtkGestureClick is unusable here (VTE claims mouse sequences; a claimed sequence cancels other gestures, so `released` never fires — hence `EventControllerLegacy`), so click sequences are recognised by GTK's own time/distance rule. Ctrl+Shift+C is the explicit route and skips the age gate: on a remote pane VTE has no selection of its own (tmux owns the mouse, except under Shift), so it takes the newest buffer whatever its age — which is also the only way a copy-mode `y` (no mouse gesture to hang off) or an **agent's** OSC 52 copy is collected. `set-clipboard` stays `on` for that last reason — agents copy by emitting OSC 52 and reporting success, and with `off` tmux discards it (verified against 3.3a), so the agent's "copied!" is a lie. The cost is tmux re-emitting OSC 52 outward to every attached client, which VTE ignores but a kitty/foot attached to the same (deterministic) session name would honour; that is the tolerable half, staleness is not. The session-exit replay of `capture-pane -e` strips OSC 52 in its awk pass — replayed bytes are parsed exactly like live output, so a clipboard-set sequence in the scrollback would be re-executed on exit. The reverse direction too: pasting (Ctrl+Shift+V) while the clipboard holds an image uploads the PNG to the host (`remote::upload_image` → `/tmp/.tuxflow-img-*.png`) and types the path into the terminal — agents treat image paths as attachments. Voice input for remote agents works the same way as the clipboard shim (remote/mic.rs, opt-in via Settings → Tools → Agents): Claude Code records by spawning `arecord -f S16_LE -r 16000 -c 1 -t raw -q -` and reading raw PCM off stdout, never checking that the binary is real, so a fake `arecord` in `~/.local/bin` reads the same stream off `~/.cache/tuxflow/mic.sock`, which `ssh -R` forwards to a local `UnixListener` that answers each connection with a real recorder. Hold-to-talk needs no key-release reporting (VTE has none — kitty-protocol support is still unmerged): it counts 5 space *repeats* arriving <120 ms apart, and auto-repeat is just bytes, so it survives VTE + ssh + tmux untouched. `~` is not expanded by sshd in an `-R` listen path, so provisioning resolves `$HOME` in the same round trip that installs the shim; it also `rm`s a stale socket first, because the host's `StreamLocalBindUnlink` default leaves one behind and sshd then refuses to bind ("remote port forwarding failed"). Bridges are keyed by host (projects share one) and torn down on quit — remote processes deliberately outlive TuxFlow, but a live microphone must not. Detected ports auto-tunnel via `ssh -N -L` (remote/tunnel.rs) — every local port seen in output, remapping to a free local port on collision (badge/URL show the local port); Ctrl+click rewrites the clicked text through the same map before opening (`rewrite_clicked_url` via the per-process `url_rewriter` hook) — the terminal shows the host's port, which locally is dead or *another project's* forward, and the lookup is `ensure()` so a click also creates or revives the forward it is about to open; tunnels use dedicated connections (NOT the mux — a forward requested via a mux client survives in the master after the client dies) with PDEATHSIG so they can't outlive the app. Output scanning only sees what a process *prints*, which TUI runners break outright: Laravel 13's `php artisan dev` draws `@laravel/multiplex`, a tabbed TUI rendering only the selected tab — a run parked on `vite` never shows its server URL, one parked on `server` never shows Vite's. The missing port is not wrapped or truncated, it is absent, so no amount of parsing recovers it. `remote/ports.rs` asks the host instead: tmux session → `list-panes -F '#{pane_pid}'` → descendants → `ss -ltnpH`, one round trip for all of a project's sessions, joined in Rust (testable) rather than in shell. Walk by **ppid, not session id** — multiplex starts each child under its own `setsid`, so `pgrep -s <pane_pid>` returns multiplex and nothing beneath it. The reply labels each session by *index*, never name: a name must be shell-quoted to reach tmux safely and those quotes are literal inside the echo, so a name-labelled reply comes back wearing them and matches nothing. Everything found forwards **1:1** (`ensure_exact`, not `ensure`) — a remote dev server hands the browser its own address (Vite bakes its port into `public/hot`), so a remapped forward listens where nothing knocks; a taken local port is a hard failure, not a remap. The poll resets to 2 s whenever it opens a new forward and backs off to 30 s once a run has settled; forwards drop when a project's last process stops, so the next run rediscovers rather than inheriting stale ports. `remote/vite.rs` (laravel-vite-plugin's `public/hot`) remains the fallback for hosts without tmux, where the inline exec leaves no pane to walk down from. Scanning still picks the badge URL — this only decides what gets tunnelled. Note `public/hot` is removed by the plugin only on *graceful* shutdown, which is why stop interrupts before killing. Git UI works remotely (`git_changes_dialog::git_command` routes through ssh; `.git` probe runs on a worker thread). Editor button uses `code --remote ssh-remote+host` for code-family editors (util/editor.rs). Remote paths autocomplete over ssh from one place (`ui/path_completion.rs` — debounced `ls -1dp`, generation-guarded so a slow probe can't overwrite the rows for what is being typed now): inline under the Add Remote dialog's path field, and as the whole content of the on-host icon picker (`ui/remote_icon_picker.rs`). The icon picker is a dialog of its own because an inline list there is unusable — the Icon row sits partway down a scrolling preferences page with the Commands section under it, so the list gets only the sliver between the entry and the sheet's bottom edge, and growing the dialog buys a row or two and no more. Still gated off for remote: file watcher, icon detection, MCP, live re-detection in Edit Project. Remote probing (config/detection/git) runs on worker threads via `util::worker::run` — never block the GTK main thread with ssh, and never poll a channel from `idle_add_local` (it busy-spins the main loop). Startup loads of unreachable remote projects notify once and retry in the background with capped backoff (`ProbeError::Unreachable` vs `Invalid` decides retryability)

## Config Files

- **Project config:** `tuxflow.toml` in project root (optional, version-controlled)
- **Global settings:** `~/.config/tuxflow/settings.toml` (appearance, keybindings, notifications)
- **Project state:** `~/.config/tuxflow/projects.toml` (open directories, custom commands, process order, UI state)

Every map serialized into these files is a `BTreeMap`, never a `HashMap` — `HashMap` iteration order is randomised per process, so each save rewrote the whole file in a fresh order and a one-key change surfaced as a whole-file diff (these files get tracked in dotfiles repos). `Vec` fields like `directories` and `process` keep user-defined order. `tests/projects_roundtrip_test.rs` pins the invariant.

## Formatting

Always run `cargo fmt --all` after changing Rust code, before committing.

## TODO

- **GTK→iced migration** — M0–M2 done (2026-08-26, branch `iced-migration`): workspace split, tuxflow-core extracted (121 tests); tuxflow-iced has the category sidebar, full process lifecycle on the alacritty backend, auto-restart via pure unit-tested `plan_after_exit`, and REMOTE projects (`tuxflow-iced ssh://host/dir`): core probe on a worker (config/detection over SshFs, Unreachable=retryable), spawn through `wrap_remote_command` into host tmux sessions, stop = `remote_kill` + fresh-next, exit-255 = endless capped reconnect (never a crash), reattach of live sessions at startup, `scan_output_wrapped` badges, auto-tunnels with collision remap + URL rewrite; probe_remote extracted to core (`remote/probe.rs`, GTK keeps the icon half). E2E-verified against a local user-mode sshd sandbox (detach survival, reattach with identical session_created, severed-link reconnect, curl through the remapped forward). M3 done same day: host-side ports poll for TUI runners (`session_ports` on a 2s→30s cadence, `ensure_exact` 1:1 forwards — poll only tunnels, scanning alone owns badges), one-shot auto-open (badge-final or 5s provisional grace, armed only by user starts), Ctrl+click through `rewrite_clicked_url`+`ensure` (fork patch 12 moved link opening to the embedder — `Action::OpenUrl`), and the composer bar under agent terminals. M4 done same day: multi-project workspace (Vec<ProjectState> with stable ids — timer events must never land on a reused vec index; per-project ports/tunnels/poll), projects.toml as the shared persistence with the GTK app (same load policy via pure `merge_saved`: custom commands override same-named detection, deletions filtered, saved order applied — unit-tested), add-project/add-command/add-agent inline forms (custom commands persist), CLI args join the workspace, closing a project detaches its remote sessions, Catppuccin Mocha terminal palette + local/remote accent dots (tuxflow-iced/src/theme.rs). M5 done in three batches + finish-up (2026-08-26): scrollback search bar (fork patch 8), app chords from shared KeybindingsSettings (reserved per-terminal via `Passthrough`), command palette, edit/delete process incl. working-dir field, SSH section, desktop notifications, git status chip (core `remote/git.rs`, active project only: on switch / on Ready / 20 s tick), recent-first project sort, Alt+Shift+Up/Down reorder persisting process_order, OSC titles in the toolbar, toolbar ↗ open chip; the .deb ships `tuxflow-iced` as "TuxFlow Preview". `tuxflow-iced/TESTING.md` is the walkthrough guide. Test-drive batch (2026-08-26, from Nikola's live feedback): sidebar truncation via `clipped_label` (Fill + clip container — iced text paints past its bounds otherwise), GTK-parity sidebar width (25 % of window clamped 220–400, tracked via resize_events), Adwaita-style overlay scrollbar (invisible until hover; `theme::overlay_scrollbar`), window geometry persistence (restore at launch incl. maximized+monitor-via-position; saved on close AND debounced ~1 s after any move/resize because cargo watch kills without a close; the position is saved from a `window::position()` (winit outer_position) query, never from `Moved` events — those carry client-area coords while restore requests are interpreted frame-or-client at the WM's whim, so mixing the spaces drifts the window by the frame extents per restart; a one-shot correction ~300 ms after launch measures where the frame landed and fixes the delta, the GTK restore_window_placement trick. Bare-Xvfb caveats: winit reports `Moved` only from the WM's *synthetic* ConfigureNotify, and WM-less/metacity sandboxes show zero frame extents, so the drift can't reproduce there — verify on a real desktop), and the FULL settings window port (`settings_ui.rs`): all seven GTK pages as a full-pane view, per-change immediate save, live-apply for terminal theme/font/accents/keybindings (capture-with-conflict-refusal, `keys::chord_string` round-trips through `parse`)/notifications+sounds/composer/single-expand/recent-first/keybind hints; Ctrl+1-9 / Alt+1-9 switchers implemented to back the hints; shared data extracted to core so the shells can't drift (`config/palette.rs` accents+status+terminal themes with the contrast tests, `util/sounds.rs` bundled sounds registry+playback, `mcp/setup.rs` integration snippets, EDITOR_CHOICES/TERMINAL_CHOICES in settings.rs — GTK consumes the same tables). Settings rows whose consumer is still GTK-only (color scheme, letter spacing, bold weight, auto-hide, file-watch flag, mic, MCP, editor/terminal defaults) say so in their subtitles and still edit the shared file. Header-bar batch (2026-08-27, from Nikola's screenshots): the GTK cluster at the TOP OF THE SIDEBAR (a full-width strip was rejected — dead space) — sidebar toggle / sidebar filter / settings / add project, the actual Adwaita symbolic SVGs vendored into `tuxflow-iced/assets/icons/` (the iced `svg` feature cost nothing, resvg was already in the tree via the fork; recolored like symbolics through `svg::Style.color`), `theme::toolbar_icon` gives toggled buttons the persistent Adwaita wash, tooltips show the live chord strings; hiding the sidebar (Ctrl+\) collapses it to a slim vertical icon rail of the same four buttons (`view_rail` — the toggle must stay mouse-reachable), and opening the filter from the rail reopens the sidebar since the entry lives there; the sidebar filter (Ctrl+F) has GTK semantics: a name-matched project keeps every row, otherwise only matching rows stay, no match hides the card, and cards render open while a query is active. Same batch, the sidebar lifecycle controls (design round: 6-variant artifact, Nikola chose F "slide-in bare glyphs" over the chip baseline): process rows reveal play (idle) / restart+stop (running) / cancel-stop (coming back), project headers reveal start-all-marked (`start_with_project`, GTK's spawn_project_group) / restart-all-running / stop-all (also cancels restarts); rows with a live URL carry a standing ↗ (`OpenBadgeFor` — `browser_url` is already tunnel-remapped). The reveal is NOT `iced::widget::hover` (its top layer can't make base content yield, and bare glyphs need the ⌃N hint/counter pill to step aside): each row is wrapped in `mouse_area` with RowEnter/RowExit maintaining `hovered_row: Option<(project id, Option<index>)>` (None = header), and the view renders the cluster inline — hint/counter omitted while hovered. The 140 ms glide is a generation-stamped 16 ms tokio-sleep tick chain (the restart-timer idiom) advancing `hover_anim`; ease-out progress drives icon-tint alpha and a padding swap (left 6·(1−p) / right 6·p — constant sum, so the clipped name next to it never wobbles). RowExit only clears state if it still owns it (enter of the next row can arrive before exit of the last). Nested buttons are safe: iced's button checks `is_event_captured` after updating content, so an inner glyph click never also selects the row. Round 5 (Nikola: "you did a very bad job of following GTK logic/structure" — the header ✕ closed projects instantly where GTK gates removal behind a context menu + AlertDialog): ✕ REMOVED, right-click CONTEXT MENUS ported with GTK's exact sections — project header: Start/Stop/Restart All | New Terminal (was the card's "+ terminal" pill, relocated on Nikola's ask) / Open in Editor / Copy Path (scp-style host:dir for remote) | Remove Project; process row: Start / Stop toggle, Restart, Resume Session (agents with a `resume_command_for` entry) | Open in Browser (URL rows) | Edit Command / Copy Command | Delete Command — destructive items red and behind GTK's confirmation texts verbatim (centered card, Cancel + filled-red commit; Esc/backdrop cancels). Mechanics: `mouse_area::on_right_press` carries no position, so the cursor is tracked from `listen_with` CursorMoved into `self.cursor` (accept the per-move update; view diffing absorbs it); menu = stack overlay (transparent click-away backdrop + `theme::menu_card` at the click point, edge-clamped), items dispatch `Event::MenuAction(Box<Event>)` which closes-then-forwards; Esc cascade order is confirm → menu → settings → palette → search → filter. Resume uses `ProcessEntry.command_override` — a one-shot taken by `spawn_settings`, so a later crash restarts the CONFIGURED command (GTK's spawn_with_command_override). `util/editor.rs` moved to core verbatim (only used core types) and `AgentKind`/`resume_command_for` extracted to core `util/agents.rs` — GTK re-exports both, zero call-site churn. Menu items NOT ported on purpose: Edit Project (dialog missing), Redraw (a VTE workaround with no analogue); Clear Output sat in that list only until fork patch 17 gave the fork a `clear()` — it is the status bar's Clear button now. Maximize-restore fix (2026-08-27, Nikola: size not remembered around maximize): winit's PRE-MAP maximize request is silently dropped on X11 (verified under metacity — `Settings { maximized: true }` produced a floating window at the saved size), and the failed restore then compounds: the floating window's next debounced save overwrites `maximized = false`, losing the state entirely. Fix = `RestoreMaximize`: when settings say maximized, re-request `iced::window::maximize(id, true)` ~250 ms after launch, once the WM has mapped the window (same trick family as RestoreSettle). The save side was already right — `save_window_state` skips width/height while maximized, so the floating size survives and unmaximize returns to it; if a slow WM applies the kick after the first debounced save recorded maximized=false, the maximize's own resize event schedules another save that self-heals. E2E-verified under metacity-Xvfb: maximize → kill → relaunch comes up MAXIMIZED → titlebar-restore returns the exact floating size. Esc in the filter follows the shell idiom — iced's text_input consumes the first Esc to unfocus itself (there is no on_escape hook), the second closes; palette and scrollback search behave identically, and routing captured Esc through `listen_with` to "fix" it would misfire on a terminal's vim Esc, which is also captured. Claude-keys batch (2026-08-27, Nikola: Ctrl+V and Alt+Enter dead in Claude Code): vendor patch 14 fixes the fork's keyboard fidelity — Alt+Enter now sends `\x1b\r` (upstream had NO binding and the named-key path has no text fallback, so the chord wrote NOTHING to the pty), unmatched Alt+text gets the ESC prefix (xterm metaSendsEscape, the VTE/alacritty default; single-byte-guarded so AltGr-composed characters pass unmangled), and Ctrl+U sent 0x51 'Q' for vt100's 0x15 NAK — an upstream transposition that broke shell kill-line. Ctrl+V is GTK's hardcoded remote-agent branch ported: a raw ^V would have the agent read the HOST's clipboard (Claude answers "No image found in clipboard" — the clipboard lives on this machine), so start() rebinds Ctrl+V→`BindingAction::Paste` on remote AGENT terminals only (text pastes bracketed from the local clipboard, exactly `terminal.paste_clipboard()`); an image-only clipboard leaves the widget nothing to paste, so the uncaptured chord falls through to the Hotkey handler, whose paste_image arm now also accepts shiftless Ctrl+V gated by `remote_agent_selected()` — the guard keeps a stray unfocused chord from typing into a terminal it was never aimed at. Local agents keep raw ^V on purpose (they read the real clipboard themselves), non-agents keep shell literal-insert. Round 2 (Nikola: text pastes, images do nothing): the fall-through never fired — iced's X11 clipboard backend (clipboard_x11) maps an owner's conversion refusal (SelectionNotify property=None, the answer an image-only clipboard gives a UTF8_STRING request) to `Ok("")`, NOT an error, so the widget saw `Some("")`, wrote an empty bracketed paste and CAPTURED the chord (probed empirically under Xvfb: image-only owner → text read `Ok` len=0, arboard image read fine). Fork patch 15: Paste and middle-click-PRIMARY treat an empty read as nothing-to-paste — no write, no capture. This also un-broke Ctrl+Shift+V image paste, which had the same dead fall-through on X11. Migration lesson: an iced `clipboard.read()` of `Some("")` means "the owner refused text conversion", not "empty string on the clipboard". Sidebar-polish batch (2026-08-27, from Nikola's screenshots): the card's vertical rhythm — the 3 px group gap LEADS each category and one sits under the project header, where before it trailed each category. A separator after the *last* one is padding, and it left the card bottom-heavy (13 px under the last row against 8 px above the header); the header needs one of its own because its button pads `[2, 4]` against a row's `[5, 9]`, so the title sat tighter to the first row than the rows sit to each other. Project state is TWO orthogonal signals, as in GTK where they are two unrelated CSS classes: an accent border RING plus an accent TITLE = something inside is running (`.project-has-running` and its `.project-name` rule), the corner-to-corner gradient = this is the active project owning the main pane (`.project-active`). Shipped the other way round first (gradient = running) and Nikola swapped it — the ring is the more GTK-faithful mapping anyway, since `.project-has-running` *is* a border. GTK's ring is a 2 px left edge and iced borders are per-side all-or-nothing, so ours circles the whole card at alpha 0.35 for the same weight of ink. GTK's `.project-active` ALSO washes the header row, and that one is deliberately dropped: in GTK the card behind it is undecorated, whereas here the active card already wears the gradient, so a standing wash on top of it boxes the title inside its own card (Nikola, on the screenshot). Hover keeps the wash. `has_running()` is wider than the `running()` behind the counter pill: it counts `Restarting` (as GTK's `project_has_running` does) and `Reconnecting`, because a remote process whose tmux session is alive on the host must not darken the card mid-reconnect. The header's hover wash moved off the title button onto a container wrapping the WHOLE header row (`theme::project_header`; the button is now transparent, `theme::header_title`, a hit target only) — it stopped short of the lifecycle glyphs and read as a box floating inside the card. It must be driven by the tracked `hovered_row`, NOT by `button::Status`: the glyph cluster is a *sibling* of the button, so pointing at the glyphs never reaches it and the strip stays dark exactly where the pointer is. Cost: no distinct press feedback (GTK has no `:active` rule on `.project-row` either). Sidebar toggle (Ctrl+\) now GLIDES between rail and full width, 180 ms ease-out; the tick-chain idiom is extracted into `Anim` (progress + generation stamp — the field is `stamp`, `gen` being a reserved keyword in edition 2024) and the hover reveal moved onto it, so the ease and frame math exist once. Three things that look wrong done naively: the content is laid out at the FULL width in an inner container and clipped by an outer one carrying the animated width — laying it out *at* the animated width re-wraps every `clipped_label` and re-flows every card each frame, so the cards visibly rearrange themselves mid-slide; the rail only swaps in once the collapse lands, since it is a different layout (the same four icons stacked, not in a row) and an early swap reads as a flicker; and reversing mid-glide must restart at `1 − eased`, which needs the ease INVERTED (`t = 1 − sqrt(1 − f)`) — the obvious `1 − t` jumps, because `eased(1 − t) != 1 − eased(t)`. `view_rail` is pinned to `SIDEBAR_RAIL` rather than left to its content so the glide's floor and the rail agree by construction. Unit tests for all three ramp behaviours live in main.rs. Cost: the main pane resizes ~11× per toggle, so terminals take that many SIGWINCHs — the same thing a window drag-resize does, but the first place to look if a TUI redraws oddly after a toggle. Working-agent sweep (2026-08-27, Nikola's ask: move the card gradient while an agent is busy): "is this agent working?" now lives in core's `util/activity.rs` — turning ON needs a repaint BURST (≥3 in a 2 s sample; a working agent redraws its spinner continuously, so the few trailing repaints after it finishes can't flap the signal), turning OFF needs 4 s of quiet (an agent thinking between tool calls must not read as done). Both shells feed it the same thing — VTE's `contents-changed` in GTK, alacritty's `Wakeup` in iced — and GTK's spinning dot was moved onto it, so the tuned constants exist once. While any agent in a project produces output the card runs a 2.6 s pass, and the signal is deliberately SPLIT across two surfaces: a band of accent light travelling the card's existing 135° diagonal, plus a cosine breath in the border ring (0.22↔0.60 against a 0.35 rest), in step. The two halves are gated ASYMMETRICALLY (Nikola, 2026-08-28): the RING breathes on every card with a working agent, the BAND rides the ACTIVE card only. The band is a modulation of the active card's wash, not a signal in its own right — on a background card there is nothing lit under it, so a lone band crossing a dark card was the only motion in an otherwise still sidebar and pulled the eye to the project you are *not* looking at, while "something over there is busy" is exactly what the ring says without moving anything under a row label. So `card_gradient` returns `None` for any unselected card (it is flat however busy it is) and `ring_alpha` never sees `active` at all. All of it went in the band at peak 0.10 first and Nikola rejected that — on the ACTIVE card the band lands on the corner the static wash has already lit, and 0.10 + 0.10 under a row label "looks like it's going over the text". It measured 9:1, so this is not a contrast bug: a bright thing sliding under text reads as interference whatever the ratio. Moving the carrying motion to the border, where there IS no text, is what let the band drop to 0.035 — present, but only tinting a surface as it passes. Chosen off a live calibration bench (an artifact running the real `Sweep::tick` math at the real cadence, with sliders and a contrast readout — the same reject-and-retry loop as the lifecycle-controls round). Mechanics worth keeping: the band travels from −half to 1+half so it sits OFF-card at both ends and the phase wraps 1→0 with nothing lit — no seam to hide — and a card only LEAVES the sweep at a pass boundary (`ProjectState.sweeping`, set at the activity tick, cleared in `Sweep::tick`'s wrap arm), so a finished agent's light glides off the edge instead of vanishing mid-card; the ring's breath is cosine, not the band's triangle, because a ring has no position to give away the turn and a linear ramp reads as a flicker there. iced's `Linear::add_stop` writes at the sorted index WITHOUT shifting, so a stop added out of order silently overwrites its neighbour, and everything past the 8th is dropped — `card_gradient` sorts and dedupes, and is ≤6 stops by construction (the wash's breakpoints plus the band's three). The chain runs at 20 fps, not 60: every frame repaints the WHOLE window (GTK's equivalent spinner is a 14 px cairo widget that redraws alone) and it runs for as long as an agent stays busy, which is minutes — measured on release under llvmpipe beside a terminal printing at 20 Hz, 30 fps cost ~25 % of a core over the same app with the sweep off, 20 fps ~9 %, and a soft edgeless band crossing in 2.6 s moves ~7 px per frame either way. The per-process half followed (`status_dot.rs`, chosen off a 20-candidate gallery — Nikola picked "Arc sweep"): the row's status light is now a canvas, a filled disc in the status colour that shrinks to a core and grows an orbiting arc while that agent produces output. Canvas because nothing below iced's canvas tier can ROTATE — container styles animate colour, radius, size and shadow and that is the whole list, and the gallery's cheaper tiers (glyph swaps like `◐◓◑◒`, a squircle walking its sharp corner around to fake a spin) were the alternatives that avoided it. The `canvas` feature is a flag, not a dependency; iced_graphics was already in the tree. Two constraints hold it together: the widget is pinned to a fixed `SIZE` (10 px, the advance of the `text("●").size(10)` it replaced) whether or not it is animating, so a row never re-flows the moment an agent starts working and no label shifted when it landed — GTK pins its dot and its DrawingArea to a shared width for exactly this; and `TURNS_PER_PASS` must stay a WHOLE number, because the arc rides the card sweep's phase instead of keeping a timer of its own (one busy agent = one timer no matter how many rows are lit, and every working row turns in step) so a fractional multiplier would teleport the arc each time that phase wraps 1 → 0. At 10 px the room goes to the GAP rather than the ink: the first cut (core 1.7, arc 4.0, stroke 1.3) left 1.6 px between them and read as one blob turning rather than a dot with something orbiting it. Note the working mark carries LESS ink than the resting disc (~15 px² against ~32) — the motion is what compensates, which is fine in the app and worth remembering when judging a still screenshot. Kept-output batch (2026-08-27, Nikola: a finished command left the pane reading "not running"): a process's terminal now lives as long as its ENTRY, not as long as its child — GTK's one-VTE-per-process, where a terminal is the process's log ACROSS runs. `finalize_exit` and `stop()` stopped dropping it (stop calls the fork's new `shutdown`, the same SIGHUP the drop performed, minus the grid; it emits no Exit event, so the status it sets is final), and a second run respawns a PTY into the SAME grid under a dim rule (`banner::run_separator`, labelled off the status being replaced — restarted / auto-restart / reconnecting) instead of building a fresh terminal. Fork patch 16 carries the mechanism (shutdown/respawn/feed, `RESET_BETWEEN_RUNS` un-wearing alt screen + mouse + scroll region WITHOUT RIS, which would clear the history the respawn exists to keep). The part that would have shipped silently broken is `drain_on_exit` — alacritty's `hold`, and it was OFF: the loop breaks out of its child-exit arm WITHOUT reading what is still buffered, so a command short enough to print and exit within one poll (`echo`, a failing build) lost its entire output. Invisible upstream because the grid died with the child anyway; here it is exactly the run the user wants to read, and it is why the first respawn test failed with an empty screen. Exit explanations moved to core (`util/banner.rs`, shared with GTK's auto_restart.rs so the wording can't drift — GTK's `String::truncate(60)` of the command was also a latent panic on a multi-byte boundary). `terminal.is_some()` had been the app's stand-in for "is running" and is not one any more: every site had to say which it meant (`is_running()` for the stop-before-start guards, the composer, the remote-agent Ctrl+V gate), and `term_id` now identifies a TERMINAL across runs, so the image-paste guard ("a restart between paste and upload must not type a stale path into the fresh run") moved onto a new per-spawn `run_id`. The pane placeholder survives only for a process that has never run — every later status is the sidebar dot plus the exit banner in the grid. E2E-verified under Xvfb: a clean finish keeps its output, a crash loop stacks five runs each under its own red banner, manual restart appends below "restarted", stop kills the child (no `sleep` survivor) with the output still on screen. Bare-pane batch (2026-08-28, Nikola: "eliminate this topbar"): the strip above the terminal is GONE — process name, status pill, OSC title, and the ↗/edit/+command/+agent/start/restart/stop pills with it; the terminal now starts at the top of its pane. Identity moved to the WINDOW TITLE, `TuxFlow - {project}: {process}`, where the process half is `ProcessEntry::display_title()` — the OSC title while a program sets one, the configured name otherwise (capped at 70 chars: agents write sentences there). No wiring needed for it to track: iced re-evaluates the `.title` closure after every update, so an `Action::ChangeTitle` reaches the WM the same frame it reaches the state. Nothing else was lost with the pills, because each already had a second home — status is the sidebar dot and the run's exit banner, lifecycle and edit are the row's hover cluster and its context menu, ↗ is the row's standing glyph and the status-bar chip. The exception was `+ command` / `+ agent`, which the toolbar alone offered (GTK keeps them in the command palette; ours only jumps to processes), so they join New Terminal on the project header's context menu — which forced `OpenAddCommand` to start naming its project, since submit writes into the ACTIVE one and a menu can be raised on any card (it switches, as selecting a row would, and re-polls git). `theme::status_pill` went with its last caller. Status-bar batch (2026-08-28, Nikola: "why is bottom bar so much different than GTK version?" — it was never ported; the iced bar was a leftover from M5, a project label plus ONE inert git pill plus the ↗ badge against GTK's ten affordances, and GTK's bar is a toolbar of ACTIONS where ours was a read-only strip). Now GTK's `status_bar.rs` in the same order — left: the remote glyph (tooltip `host:dir`), `{project} {r}/{t}`, `·`, `Total {r}/{t}` whose tooltip lists every running process per project (the counter that says something is alive in a project you are NOT looking at); right: the two git chips SPLIT as in GTK — a sync chip (`⎇ branch` + ↓amber/↑green, click = fetch + ff-pull + push) and a changes chip (`+N −M` compact, click opens Git Changes) — then Focus / the ↗ badge / Clear / Stop / Restart. Stop is HIDDEN rather than disabled when the selection isn't running (GTK parity — you can't stop what isn't going), and Clear/Stop/Restart appear only with a selected process, so the bar's right end changes width with state: anything scripting clicks against it must locate the buttons, not hardcode x. The sync chip's counters stand DOWN while a sync runs (pre-sync numbers beside a spinner read as "the sync did nothing") and a failure opens a `notice` card — one heading, one OK, top of the overlay stack above `confirm`, first in the Esc cascade — carrying git's stderr and GTK's verbatim "Open Git Changes to resolve it manually." Git colours are SEMANTIC constants in theme.rs mirroring style.css (`GIT_ADDED` #73c991, `GIT_REMOVED` #f14c4c, `GIT_BEHIND` #d29922, plus MODIFIED/UNTRACKED for the file badges) and deliberately NOT `accent_for` — the accent is user-settable and "added" must not move with it, even though the shipped local accent happens to be the same green. `theme::primary` gained a `Disabled` arm in the same pass: a filled accent button with no `on_press` was painted fully lit, so Commit-with-no-message looked armed and did nothing. Underneath, the WHOLE git plumbing moved out of GTK's `git_changes_dialog.rs` into core `remote/git.rs` (~250 lines: `changed_files`/`status_hash`/`load_diff`/`highlight_diff`/`query_diffstat`/`commit_all`/`push`/`pull`/`sync_with_remote`/`has_git_repo`/`current_branch`), so **syntect is a core dependency now, not a GTK one** — highlighting is emitted as plain `(line, byte_offset, len, hex)` tuples, which GTK turns into TextTags and iced into text spans, and neither toolkit is baked into the answer. GTK re-exports the eight names window.rs imports, so its call sites are untouched. The Git Changes DIALOG is ported too (`git_view.rs`), as a full-pane view like `settings_ui` rather than a modal — a diff wants the window, which is why GTK's dialog opens at the parent's full size. Points worth keeping: the diff is ONE `rich_text` with per-span colour and background, not a widget per line, because iced does not virtualize scrollables and the upstream cap is 5000 lines — the trade is that a `+`/`-` band paints behind its TEXT rather than out to the right edge the way GTK's line tag does; EVERY async arrival is generation-stamped, since a remote round trip is seconds long and the user can switch files, refresh or pull while one is in flight (an unstamped reply paints the previous file's diff over the current one); the file selection is held by PATH across reloads, not index, or a file leaving the list above the selected one silently moves the selection to its neighbour; a write action owns its own counter until it reports, so the 2 s poll can't repaint the pre-push number over a push in flight; and the view is closed centrally at the TOP of `update()` when the active project no longer matches, because `self.active` moves from eight different places and a stale view would otherwise show another project's repo while the sidebar and status bar say something else — and keep polling it. Fork patch 17 (`clear()`) exists because `feed` is documented safe only BETWEEN runs while Clear is pressed AT a running process: it mutates the grid under the `FairMutex` the PTY loop already takes, and the mode order is the REVERSE of `respawn`'s — `ClearMode::All` before `Saved`, since on the primary screen alacritty implements All as "scroll the viewport into history" (the xterm behaviour that makes `clear` preserve scrollback), so wiping history first leaves the screen you just cleared sitting in it. E2E-verified under metacity-Xvfb against a scratch repo with a real `origin`: chips render `+3 −1` matching `git diff --shortstat`, the view lists `M`/`U` files with syntect colour and `--no-index` diffs for untracked ones, Commit really commits (log + clean tree), the sync chip really pushes (origin SHA moved), a diverged history raises the notice with git's own hint text, Esc closes the view, and five Clears fired mid-`ping` left the app up, the child ALIVE and output resuming at `icmp_seq=89` with the history gone. Diff-pane batch (2026-08-28, Nikola picked four treatments off a live toggle bench rather than from prose — full-bleed bands, split +/− gutter, word-level highlight, dim removed; hunk breaks, monospace, line numbers and a Catppuccin syntect theme were offered and declined). Full-bleed is what forced the structure: a span's `background` paints behind its GLYPHS, which is what left every band with a ragged right edge, so the tint moved onto a per-line `container` (`theme::diff_band`) and the pane is now one row per line — `container(row![marker, rich_text])` — reversing the "ONE `rich_text` for the whole diff" note above. Affordable because iced caches shaped text: `Paragraph::update` early-returns on `Difference::None`, so a static diff re-walks its layout tree each frame but re-shapes nothing. The horizontal scrollbar went with it — `Length::Fill` inside a horizontally-scrollable resolves against the VIEWPORT, so a full-width band stops at the right edge and scrolling reveals untinted line past it; long lines wrap (`WordOrGlyph`) instead. The marker is a separate widget from the code, so `DIFF_LINE_HEIGHT` is pinned on BOTH — left to the default they are free to disagree about how tall a row is, and the gutter drifts down a long run. Word-level lives in core (`word_diff_ranges`, emitted as `(line, offset, len)` with the same marker-inclusive offsets as `highlight_diff`, so GTK can consume it too); the view merges the two independent range lists by letting the syntax walk drive and re-cutting each of its pieces by any word range crossing it. Three things measured rather than guessed, each of which shipped wrong first: the similarity gate divides by the SHORTER line (on real samples `/max` scored a genuine rewrite 0.23 against an unrelated pair's 0.06 — the gate landed between the good cases; `/min` separates them 0.41 to 0.10); the two runs are ALIGNED before pairing instead of index-wise, because one inserted comment shifts the added run and every removed line then diffs against its neighbour's replacement; and the alignment score drops whitespace and carries a locality discount (`ALIGN_LOCALITY`), since indentation is shared by every line in a run — six shared spaces let a `box-shadow:` five rows away outscore the `filter: var(…)` sitting directly opposite. Both mispairings are INVISIBLE in the band (the lines are fully coloured either way) and surfaced only as word marks on unrelated lines in a screenshot; each is now a unit test naming the case. Verified under metacity-Xvfb against a scratch repo, isolated via `XDG_CONFIG_HOME` so the run could not touch the real projects.toml. Add-project batch (2026-08-31, Nikola: "adding a project is not good (design wise and functionality wise), not even close to how gtk version works"). The iced form was ONE text box taking a path or `ssh://host/dir`, against GTK's chain of a native folder picker / `add_remote_project_dialog` / then one of two follow-ups. Now `add_project.rs`, a full-pane view (Nikola's pick over modal cards) walking Choose → Locate → Configure, with the sidebar `+` opening the Local/Remote fork directly (also his pick, over mirroring GTK's `+`→palette indirection; the iced palette has no action tier to hold GTK's two "New project…" items, so that stays the only route in). **There is deliberately no native folder picker.** rfd would not have linked GTK in (its default backend is xdg-portal + wayland), but the dialog you SEE is drawn by the desktop's portal backend, which on GNOME is GTK out-of-process — and it was the wrong DRY call regardless: a portal can only browse THIS machine, so the remote half needed an in-app browser anyway and rfd would have meant two UIs for one job. Instead `remote::fs::list_local_dirs` is a local twin of `list_remote_dirs` with a deliberately identical contract (absolute paths, trailing `/`, mid-name prefix match, sorted, capped) — including glob's dotfile rule, since a bare `*` does not match `.config` and without that the local half would offer `.git` on every directory while the remote half would not — and `list_dirs(host: Option<&str>, …)` dispatches, so ONE completion widget drives both halves. Completions are generation-stamped (a slow listing must never paint over what is being typed now) and debounced at 250 ms only when remote, since a local `read_dir` is not an ssh round trip. The Configure stage is GTK's two follow-up dialogs merged behind its own rule, now shared as `detector::needs_command_selection` (GTK's window.rs calls it too, so the threshold can't drift): a `tuxflow.toml` project or ≤5 detected commands gets the plain rename, more gets the per-stack switch list with Select/Deselect All and the live "Add N Commands". The half that would have shipped silently broken is the PERSISTENCE, and it is not symmetric — a deselected process is marked deleted, but a SELECTED one usually needs nothing because `open_project` re-detects and finds it again. Except the add flow detects with the FULL `detect_stacks` while the startup loader re-detects CONSERVATIVELY, so a selection outside that subset has no source to come back from and silently vanishes on the next launch; those are persisted as custom commands instead (`detector::conservative_names`, also now shared with GTK's `finalize_project`, which is where the rule was discovered). E2E-verified under metacity-Xvfb with an isolated `XDG_CONFIG_HOME`: picking 5 of 7 detected scripts wrote `build`/`format` to deleted_processes and `lint`/`preview`/`typecheck` to custom_commands (leaving conservative `dev`/`test` to re-detection), and a RESTART came back with exactly those five. Also: duplicates now say "Already open: {name}" rather than GTK's silent early-return, which reads as a dead button; the two add forms joined the Esc cascade (neither was in it); `widgets.rs` holds the Adwaita group/row/switch furniture that was private to `settings_ui`, generic over the message type so the two can't drift; and `theme::toggler` puts switches in the accent instead of iced's default blue. Add-agent batch (2026-08-31, Nikola: the modal should offer a list of the popular agents, keep command/working-dir/name, and drop open-in-browser): the agent form now leads with a preset list (Claude Code / Codex / Gemini CLI / OpenCode / Aider) that fills the command and a collision-free name, all still editable — GTK makes you choose between a palette preset with NO options and a blank custom form, so this is the one place the iced flow is deliberately BETTER rather than faithful. The table is core's `util/agents::AGENT_PRESETS` and GTK's palette is generated from it now, which is also how GTK gained Aider. It is deliberately NOT keyed on `AgentKind`: that enum earns its variants by having behaviour behind them (a per-agent notification sound with a settings field, `resume_command_for`, OpenCode's own-notification suppression), so an agent with no integration rides the table as a preset and takes `Unknown`'s defaults rather than becoming a half-wired variant — a unit test pins which presets map to which kind so that stays a choice and not a drift. The selected preset is DERIVED from the command (`typed == preset.command`) rather than stored, so editing the command to `claude --model opus` falls back to "custom" for free; the one piece of state needed is `name_touched`, since a preset pick must not overwrite a name the user typed (an emptied field hands it back to the preset). `unique_agent_name` steps `claude` → `claude-2` → `claude-3` instead of GTK's `claude-a1b2c3d4` uuid suffix, which is unreadable in a sidebar row — and it has to dedupe, because the submit path refuses a duplicate name outright and a colliding suggestion would read as another dead button. Open-in-browser is dropped for agents only (an agent serves no port), on both the add and edit forms, and the stored flag is left alone rather than forced off; the preset list is hidden while EDITING, where the command is the thing being edited and a preset row would silently overwrite it. Remaining before the flip: DnD reorder / local trio (file watcher, MCP, update chip) as demand dictates. Migration lessons so far: reserve app chords in the terminal via `BindingAction::Passthrough`; never store grid coordinates across syncs (the grid rotates under new output); a heuristic whose failure is invisible in the coarse signal (the band) has to be checked against the fine one (the marks) on a real diff — unit tests written from the same intuition that wrote the heuristic will agree with it
- **File upload to remote terminals** — Extend the image-paste bridge to arbitrary files: Ctrl+Shift+V with a file on the clipboard (`text/uri-list`) uploads over the existing ssh connection and types the remote path, like images do today (`remote::upload_image`). Plus a `GtkDropTarget` on the terminal view so drag-and-drop works too — local projects get path-paste-on-drop natively from VTE; remote should match
- **Split terminal view** — Currently `gtk4::Stack` (one at a time). Would need `gtk4::Paned` for side-by-side
- **Composer inline images** — Composer delivers attachments first, then text. Nicer: `insert_paintable` thumbnails at the cursor, send walks the buffer and interleaves text chunks with Ctrl+V per image so `[Image #N]` lands where it was placed. Replaces the chip row; needs thumbnail scaling, upload-in-flight gating on send, identity-based (not order) texture→path matching
- **Tests** — Core modules covered (config, detector, port detector, log buffer). UI and process management untested (require GTK runtime)
- **CI** — `.github/workflows/ci.yml` (fmt/clippy/test on push + PR) and `release.yml` (on `v*` tags: builds the .deb + tarball, publishes the GitHub Release, force-pushes the signed apt repo to gh-pages). `scripts/release.sh` only bumps, tags and pushes — the artifacts come from the tag, so they land a few minutes later; a release checked too early looks half-published when it is just mid-run
