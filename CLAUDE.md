# TuxFlow

Linux-only desktop app for managing dev environments — processes, AI coding agents, and terminals from one window. Inspired by [SoloTerm](https://soloterm.com/) but native to Linux.

## Stack

- **Language:** Rust
- **GUI:** iced 0.14 (wgpu/tiny-skia), Adwaita-styled furniture drawn by the app
- **Terminal:** the vendored `iced_term` fork over `alacritty_terminal` (`vendor/iced_term/VENDOR.md`)
- **Async:** tokio
- **Config:** TOML (serde + toml crate)
- **MCP:** rmcp (Rust MCP SDK, Unix socket transport)
- **File watching:** notify v7

## Build & Run

```bash
# System deps: a C toolchain only (Ubuntu 24.04: build-essential); no toolkit packages.

# Build & run the app (release — debug misrepresents terminal latency)
cargo build -p tuxflow
cargo run --release
cargo run --release -- /path/to/project
cargo run --release -- ssh://host/dir
```

## Project Structure

Cargo workspace since migration step M0 (2026-08-26; branch `iced-migration`, merged 2026-09-15). The packaging flip (2026-09-15) made the iced shell the ROOT package `tuxflow` — `/usr/bin/tuxflow`, the one `com.tuxflow.TuxFlow` desktop entry, a .deb whose depends are `$auto` alone. `tuxflow-core` is the GUI-independent heart. The GTK app was deleted the same day (last present at `1db72a9`, as `tuxflow-gtk/`; `git show 1db72a9:tuxflow-gtk/src/ui/window.rs` when a batch note below cites GTK behaviour), and CI no longer installs any toolkit package. The shell is built on the vendored iced_term fork (see `vendor/iced_term/VENDOR.md` for the patch-by-patch history; the spike in `spike/iced-term-spike/` is the frozen de-risking record with its own lockfile, consuming the same vendor).

```
tuxflow-core/              # GUI-independent (no GTK/iced/VTE types allowed)
  src/
    config/
      schema.rs            # Serde structs for tuxflow.toml (ProcessConfig, ProcessCategory, etc.)
      loader.rs            # TOML loading, validation, defaults
      keybindings.rs       # Serde half only: ShortcutAction, KeybindingsSettings (strings)
      palette.rs           # Shared accent/status/terminal-theme tables + the contrast tests
      projects.rs          # Project config management (~/.config/tuxflow/projects.toml)
      settings.rs          # Global settings (~/.config/tuxflow/settings.toml)
      state.rs             # Machine-local state (~/.local/state/tuxflow/state.toml): geometry, last_used, expanded
      persist.rs           # read_toml / write_toml (atomic tmp+rename) shared by the three files
      ssh.rs               # SSH config parser (~/.ssh/config host extraction)
    detect/
      detector.rs          # Tech stack auto-detection (package.json, Cargo.toml, etc.) over ProjectFs
    remote/
      mod.rs               # ProjectLocation (Local/Ssh), sh_quote, ssh ControlMaster options, remote command wrapping
      fs.rs                # ProjectFs trait: LocalFs (std::fs) / SshFs (ssh exec over shared connection); dir completions
      git.rs               # Git plumbing both shells consume: status/diffstat, diffs + syntect + word-diff, commit/push/pull/sync
      icon.rs              # Fetch a remote project's icon into ~/.cache/tuxflow/icons
      mic.rs               # MicBridgeManager: ssh -R socket + fake arecord, so remote agents get voice input
      ports.rs             # What a remote run actually listens on (tmux pane → process tree → ss)
      probe.rs             # Remote project probe: config/detection over SshFs (Unreachable vs Invalid)
      tunnel.rs            # TunnelManager: ssh -L port forwards for detected ports on remote projects
      vite.rs              # Reads laravel-vite-plugin's public/hot — fallback for hosts without tmux
    mcp/
      server.rs            # MCP server on Unix socket (/tmp/tuxflow-<project>.sock)
      tools.rs             # MCP tools: list_processes, get_process_logs, start/stop/restart
      bridge.rs            # UI<->MCP thread bridge, LogBuffer (VecDeque ring buffer)
      setup.rs             # MCP integration snippets shared by both settings UIs
    util/
      activity.rs          # "Is this agent working?" signal (repaint burst on, 4 s quiet off)
      agents.rs            # AgentKind, AGENT_PRESETS, resume_command_for, unique_agent_name
      banner.rs            # Exit explanations + run separators, shared by both shells
      editor.rs            # Open-in-editor command builder (code --remote for ssh hosts)
      icon_detector.rs     # Project avatar detection + resolve policy (saved wins while its file exists)
      port_detector.rs     # Regex scan terminal output for ports/URLs
      sounds.rs            # Bundled notification sounds registry + playback
      watch.rs             # restart_when_changed: WatchSet glob matching + debounced dir watcher, both shells
      update.rs            # Release check (15-min cache), .deb download + pkexec install, binary_replaced, relaunch hand-off
  tests/                   # config, detector, log buffer, port detector, projects roundtrip, remote fs
  examples/                # detector_check, port_scan_check, remote_ports_check
src/                       # The app — package `tuxflow` (root), the iced shell; TESTING.md = walkthrough guide
  main.rs                  # App state/update/view, CLI args (paths, file:// URIs, ssh://host/dir)
  bin/tuxflow-mcp.rs       # Standalone MCP client binary (stdio ↔ project socket; core only)
  processes.rs             # Spawn settings + merge_saved
  keys.rs                  # Chords (AppKeys from the shared KeybindingsSettings)
  theme.rs                 # Palette (dark/light behind pal()), card Sweep, semantic colours
  scheme.rs                # System color-scheme via the settings portal
  settings_ui.rs           # Settings as a full-pane view
  git_view.rs              # Git Changes as a full-pane view
  add_project.rs / add_ssh.rs / edit_project.rs   # Full-pane forms
  widgets.rs               # Adwaita furniture (groups/rows/switches/avatars) generic over the message
  status_dot.rs            # Canvas status light + working arc, spinner
  dnd.rs                   # Sidebar drag-and-drop sensor widget
  notify.rs                # Desktop notifications over one lifelong zbus connection
  mcp.rs                   # Per-project MCP bridge + remote forward wiring
assets/icons/              # Vendored Adwaita symbolics embedded via include_bytes (README lists sources)
vendor/iced_term/          # Hardened iced_term fork (MIT) — VENDOR.md documents every patch
spike/iced-term-spike/     # Frozen spike record (own workspace; README = VTE-parity scorecard)
data/
  icons/                   # App icon (hicolor hierarchy) + logo sources
  sounds/                  # Bundled notification packs (embedded by core's util/sounds.rs)
  com.tuxflow.TuxFlow.desktop
  com.tuxflow.TuxFlow.metainfo.xml
```

## Architecture Notes

The batch log under TODO cites GTK files and behaviours by name (the iced port was done feature by feature against them); that code is gone from the tree since 2026-09-15 — read it from `1db72a9:tuxflow-gtk/` when a note needs it.

- **Process categories:** `ProcessCategory::Command`, `Agent`, `Terminal`, `SSH` — each gets a dedicated sidebar section. SSH connections are ordinary terminals running `ssh` commands, reusing the full process management infrastructure (auto-restart handles reconnection)
- **MCP bridge:** each project gets an ISOLATED `McpBridge::isolated()` (own maps, own socket `$XDG_RUNTIME_DIR/tuxflow-<name>.sock`), snapshots rewritten at the top of `update()` whenever a fingerprint of the process list moves, logs read straight off the grid on demand (fork patch 25 `recent_text`); remote projects reverse-forward the socket to the host with a Python shim — the MCP batch in the TODO has the whole design
- **Settings persistence:** every settings row saves immediately to `~/.config/tuxflow/settings.toml` (`settings_ui.rs`, per-change) and applies live; the file is shared with what earlier GTK-era releases wrote, so no key may be renamed
- **Accents:** one palette (core `config/palette.rs`) feeds three pickers — the app accent plus `local_accent` and `remote_accent`, the sidebar hues that say where a project lives (green / logo gold by default). The sidebar reads accents as TEXT and thin borders, so each name carries an `accent_light` twin for light surfaces and `theme::set_scheme` re-resolves the remembered names on a scheme flip; the same table carries `status_working`/`status_restarting`, whose ambers measure ~2.2:1 on a light sidebar and need dark twins. Contrast is unit-tested in core against both schemes — 4:1 for accents, 3:1 for the dots — using constants STRICTER than what the app paints so the assertions keep a margin. Unknown names (hand-edited settings) fall back to the shipped defaults
- **URL handling:** the fork detects links in the grid and hands Ctrl+click to the embedder (`Action::OpenUrl`, patch 12), which rewrites through the tunnel map (`rewrite_clicked_url`) before `open`; the status-bar ↗ and the row menu open the badge URL the same way
- **Port/URL detection (tuxflow-core/src/util/port_detector.rs):** badge priority is labeled "Preview URL:" (preferred, locks even though remote — `shopify app dev`'s admin URL beats its localhost GraphiQL) > local > provisional remote (OAuth links, upgradeable). Infra/error lines never badge ("proxy server started", "econnrefused", vite tool lines) but their ports still harvest into `seen_local` for tunnelling. Tool lines do have a *fallback* tier below `local`: a dev server's own address line (`➜  Local:  http://localhost:5173/`, matched by shape in `is_arrowed_label` — vite picks `➜` or `→` by font, so literal phrases made the badge depend on a glyph) badges when nothing else claims one, marked `tool: true` so `badge_final` stays false and the app's port still takes over when it prints. Without it a project whose API dies (bun on EADDRINUSE beside vite) shows no port, no URL and never auto-opens, since every consumer in window.rs hangs off the badge. Only *address* lines get the fallback, never the rest of the tool class — auto-opening `shopify app dev`'s proxy port is the bug that tier must not reintroduce. A stricter class (`FOREIGN_PORT_PHRASES`) is skipped *entirely*, harvesting included, because the port named belongs to someone else: vite's "Port 5173 is in use, trying another one" and — since `php artisan serve` walks `--tries` ports and echoes PHP's bind failure verbatim — "Failed to listen on 127.0.0.1:8000 (reason: Address already in use)". Both announce the retry and the port they retried onto, so dropping the line loses nothing. Match the bare phrase (`in use`), not each server's sentence around it — bun's "error: Failed to start server. Is port 3000 in use?" puts the port *inside* what used to be matched as "is in use", and bun then exits rather than retrying, so no later line corrects a badge that latched the neighbour's port. Getting this wrong is not a cosmetic off-by-one on a remote project: 8000 there is a *neighbouring project's* server, so the badge locks to it, tunnels to it (remapped, since local 8000 is already forwarded elsewhere) and the browser button silently opens the wrong app under this project's name. Match on the phrase, not the reason — the multiplex TUI clips the line to pane width, so "Address already…" is elided while "Failed to listen" survives on the same row as the port. Auto-open (one-shot per user start) fires when the badge is *final*, or after a 5 s grace for provisional-only badges — never on first-URL-seen. Final is not enough on its own: `php artisan serve` announces its port seconds before the Vite beside it listens, and a page opened in between loads with no stylesheets and dead script tags (both shells had this until 2026-09-24). So a final badge arms a 1.5 s *quiet* timer (`AUTO_OPEN_QUIET`) that every later output batch re-arms, and when it fires the app TCP-probes the badge port plus every other local port the run printed (`auto_open_probe_ports`, through the tunnel map on remote projects; both loopbacks, since Vite's `localhost` may be `::1` only), retrying every 500 ms until all answer. Both deferrals stop at `AUTO_OPEN_CAP` (10 s after the first port) so a chatty run or a dead asset server still opens; timers and probes carry an `auto_open_epoch` beside the run generation, so a firing that later output re-armed is dropped. Ink-based CLIs (shopify) hard-wrap output at width−1 with a real newline, which even `tmux capture-pane -J` can't rejoin — remote scans go through `scan_output_wrapped`, which re-joins rows of width/width−1 before parsing (local VTE joins its own soft wraps natively). A row ending on a box-drawing glyph is exempt: full-screen TUIs (`php artisan dev` → `@laravel/multiplex`) border the pane, so *every* row measures full width and joining them fuses the whole screen into one logical line — where one "Failed to listen" swallows the port announced rows below and the badge comes back empty. Width alone cannot tell a frame edge from a wrap; Ink's wraps break mid-token and never land on a border. `tuxflow-core/examples/port_scan_check.rs` replays a `tmux capture-pane` dump through this path when a badge misbehaves. Agent and SSH terminals are never scanned at all — the rule is core's `port_detector::scans_ports`, consumed by GTK's `skip_port_detection` and iced's `rescan_ports` gate alike (pinned in port_detector_test): their output is prose, so the first URL the model *mentions* would latch as the badge, and a quoted localhost port would go further and tunnel. Custom commands in projects.toml override same-named detected processes at load (a user's edit of a detected process persists there)
- **Detection baseline:** the sidebar changes only when the user changes it. A project's first load records the names detection offered (`known_detected` in projects.toml, `SavedProjects::withhold_new_detections`); later loads drop detected names outside it, so a Makefile written after the project was added does not arrive as nineteen uninvited rows on the next launch. Withheld names stay in the detection pool, i.e. Edit Project's "Detected" group — enabling there persists a custom command, which never passes through the baseline. The baseline never grows (a withheld name would walk in one launch later) and an authored `tuxflow.toml` list is exempt. Edit Project re-detects live: inline for local projects, and for remote ones via a background `detect_stacks_fs` whose result (`EditProjectDetected`, form-epoch-stamped) rebuilds the rows while keeping switches flipped during the wait
- **`TUXFLOW_CHILD=1`** env var is set on every spawned process (used to prevent recursive spawning; `update::restart` scrubs it before relaunch)
- **Updates:** the GitHub check runs at launch and again every `update::RECHECK_INTERVAL` (3 h; each `UpdateChecked` schedules the next `UpdateRecheck`, and the chain ends once a restart is pending — a window open for days was otherwise blind to every release after its start) (15-min cache in `~/.cache/tuxflow/update-check.json`, storing the latest version unfiltered so the same cache re-evaluates correctly against a changed running version). The checker, installer and relaunch live in core `util/update.rs` and take the RUNNING version as a parameter — core's own `CARGO_PKG_VERSION` is not the app's — and the workspace has ONE version (`[workspace.package]`, inherited by every package; release.sh bumps that first `version =` line), because the .deb is versioned off the root package and a shell left at its own 0.1.0 would report an update forever. The status-bar chip has two states behind a single click handler (`UpdateBadge` in status_bar.rs — re-connecting `clicked` per state would stack handlers and open two dialogs): "Update available" opens release notes + `pkexec apt-get install` of the .deb; "Restart to finish updating" goes straight to the restart prompt. The second state comes from a 30 s poll of `/proc/self/exe` — dpkg renames the new binary over ours, so the link gains a `" (deleted)"` suffix, and that is the *only* signal that the system's software manager upgraded us underneath a running window (the process keeps running the old code quite happily). Release builds only: `cargo run` replaces its own binary on every rebuild and would light the chip permanently. Restart hands off to a detached `sh` that waits for our PID to exit, since the app is single-instance and a second launch just activates the old window — bounded by `RELAUNCH_GRACE` (15 s), after which it TERMs then KILLs us and execs anyway: 0.2.3 closed its window but never exited, so the relauncher sat in its loop for hours and the button read as doing nothing (the script comes from the version being *left*, so this only protects upgrades from 0.2.5 on)
- **Remote projects:** a project's `ProjectLocation` is `Local(PathBuf)` or `Ssh { host, dir }`, persisted in projects.toml as an opaque `ssh://host/path` key (no schema migration). Remote processes spawn as `exec ssh -t <mux> host '<wrap>'` in the normal VTE pipeline; all ssh invocations share a ControlMaster connection (`$XDG_RUNTIME_DIR/tuxflow/ssh-%C`). The wrap runs the command inside a tmux session on a dedicated server (`tmux -L tuxflow`, status off, mouse on, deterministic `tf-<proc>-<fnv32>` session names from project key + process name), so connection loss and app quit only *detach* — reconnect and app relaunch reattach via `new-session -A`; the command's exit code round-trips through `/tmp/.<session>-<uid>.exit` (uid-namespaced — deterministic session names would otherwise collide across users on a shared host) so crash detection still works. Hosts without tmux fall back inline to direct exec (no persistence; `pkill -s` on the pidfile PID covers the kill). Stop interrupts the tmux session (C-c) and waits up to ~2 s for it to end before killing it, then sweeps the pidfile login session, and flags the next spawn to kill-before-create (`remote_fresh_next` — avoids racing the fire-and-forget remote kill). The grace exists so a program gets to run its own cleanup: Vite removes the `public/hot` file naming its dev server, and an orphaned one leaves the app serving asset URLs pointing at a dead port long after the run that wrote it. Everything after the initial lookup targets tmux's unique *session id*, never the name — names are deterministic and reused, so a restart respawns the same name inside the grace window and a name-targeted wait would end by killing the session it just started. Agents treat C-c as "interrupt" rather than "quit" and are expected to fall through to the kill; app quit calls `detach_all()` instead, so remote processes deliberately outlive TuxFlow. Exit 255 on a remote process is surfaced as "connection lost — reconnecting", not a crash; reconnects retry forever (backoff capped at 32 s, one notification per outage) since the session is still alive on the host. Project load probes `tmux list-sessions` and auto-reattaches processes whose sessions are still alive, so the UI never shows "stopped" for a running detached process. Mouse selections inside tmux reach the local clipboard via TuxFlow's own bridge (selection gesture on a remote terminal → newest tmux paste buffer over ssh → GTK CLIPBOARD *and* PRIMARY, change-detected by hash) — NO released VTE implements OSC 52 (verified through 0.84), so the standard escape-sequence route is a dead end. The hard problem is not fetching the buffer but knowing whether it is *ours*: `show-buffer` always answers, and answers with the newest buffer on that server, which may be the drag that just finished or an OSC 52 an agent sent half an hour ago that nothing has displaced since. Publishing the second as if it were the first is what silently reverted the user's Ctrl+C to scrollback nobody had selected — it reads as "Ctrl+C sometimes doesn't work". Three gates decide, and dropping any one brings the bug back in a different disguise: the gesture must be able to *make* a selection (`SelectionGesture` in window.rs — a drag, or a double/triple click for tmux's word/line copy; firing on every button release meant a plain click to focus a pane republished a buffer from minutes earlier), the buffer must be younger than 5 s (`fetch_tmux_buffer` returns `#{buffer_created}` next to the host's own `date`, both read on the host so a skewed local clock can't fake freshness — the window is loose on purpose, since a false negative silently drops the user's copy while the race it guards needs a gesture that selected nothing within 5 s of a program's copy), and the hash must differ from the last publish. GTK4 dropped the multi-press event types and GtkGestureClick is unusable here (VTE claims mouse sequences; a claimed sequence cancels other gestures, so `released` never fires — hence `EventControllerLegacy`), so click sequences are recognised by GTK's own time/distance rule. Ctrl+Shift+C is the explicit route and skips the age gate: on a remote pane VTE has no selection of its own (tmux owns the mouse, except under Shift), so it takes the newest buffer whatever its age — which is also the only way a copy-mode `y` (no mouse gesture to hang off) or an **agent's** OSC 52 copy is collected. `set-clipboard` stays `on` for that last reason — agents copy by emitting OSC 52 and reporting success, and with `off` tmux discards it (verified against 3.3a), so the agent's "copied!" is a lie. The cost is tmux re-emitting OSC 52 outward to every attached client, which VTE ignores but a kitty/foot attached to the same (deterministic) session name would honour; that is the tolerable half, staleness is not. The session-exit replay of `capture-pane -e` strips OSC 52 in its awk pass — replayed bytes are parsed exactly like live output, so a clipboard-set sequence in the scrollback would be re-executed on exit. The reverse direction too: pasting (Ctrl+Shift+V) while the clipboard holds an image uploads the PNG to the host (`remote::upload_image` → `/tmp/.tuxflow-img-*.png`) and types the path into the terminal — agents treat image paths as attachments. Voice input for remote agents works the same way as the clipboard shim (remote/mic.rs, opt-in via Settings → Tools → Agents): Claude Code records by spawning `arecord -f S16_LE -r 16000 -c 1 -t raw -q -` and reading raw PCM off stdout, never checking that the binary is real, so a fake `arecord` in `~/.local/bin` reads the same stream off `~/.cache/tuxflow/mic.sock`, which `ssh -R` forwards to a local `UnixListener` that answers each connection with a real recorder. Hold-to-talk needs no key-release reporting (VTE has none — kitty-protocol support is still unmerged): it counts 5 space *repeats* arriving <120 ms apart, and auto-repeat is just bytes, so it survives VTE + ssh + tmux untouched. `~` is not expanded by sshd in an `-R` listen path, so provisioning resolves `$HOME` in the same round trip that installs the shim; it also `rm`s a stale socket first, because the host's `StreamLocalBindUnlink` default leaves one behind and sshd then refuses to bind ("remote port forwarding failed"). Bridges are keyed by host (projects share one) and torn down on quit — remote processes deliberately outlive TuxFlow, but a live microphone must not. Detected ports auto-tunnel via `ssh -N -L` (remote/tunnel.rs) — every local port seen in output, remapping to a free local port on collision (badge/URL show the local port); Ctrl+click rewrites the clicked text through the same map before opening (`rewrite_clicked_url` via the per-process `url_rewriter` hook) — the terminal shows the host's port, which locally is dead or *another project's* forward, and the lookup is `ensure()` so a click also creates or revives the forward it is about to open; tunnels use dedicated connections (NOT the mux — a forward requested via a mux client survives in the master after the client dies) with PDEATHSIG so they can't outlive the app. Output scanning only sees what a process *prints*, which TUI runners break outright: Laravel 13's `php artisan dev` draws `@laravel/multiplex`, a tabbed TUI rendering only the selected tab — a run parked on `vite` never shows its server URL, one parked on `server` never shows Vite's. The missing port is not wrapped or truncated, it is absent, so no amount of parsing recovers it. `remote/ports.rs` asks the host instead: tmux session → `list-panes -F '#{pane_pid}'` → descendants → `ss -ltnpH`, one round trip for all of a project's sessions, joined in Rust (testable) rather than in shell. Walk by **ppid, not session id** — multiplex starts each child under its own `setsid`, so `pgrep -s <pane_pid>` returns multiplex and nothing beneath it. The reply labels each session by *index*, never name: a name must be shell-quoted to reach tmux safely and those quotes are literal inside the echo, so a name-labelled reply comes back wearing them and matches nothing. Everything found forwards **1:1** (`ensure_exact`, not `ensure`) — a remote dev server hands the browser its own address (Vite bakes its port into `public/hot`), so a remapped forward listens where nothing knocks; a taken local port is a hard failure, not a remap. The poll resets to 2 s whenever it opens a new forward and backs off to 30 s once a run has settled; forwards drop when a project's last process stops, so the next run rediscovers rather than inheriting stale ports. `remote/vite.rs` (laravel-vite-plugin's `public/hot`) remains the fallback for hosts without tmux, where the inline exec leaves no pane to walk down from. Scanning still picks the badge URL — this only decides what gets tunnelled. Note `public/hot` is removed by the plugin only on *graceful* shutdown, which is why stop interrupts before killing. Git UI works remotely (`git_changes_dialog::git_command` routes through ssh; `.git` probe runs on a worker thread). Editor button uses `code --remote ssh-remote+host` for code-family editors (util/editor.rs). Remote paths autocomplete over ssh from one place (`ui/path_completion.rs` — debounced `ls -1dp`, generation-guarded so a slow probe can't overwrite the rows for what is being typed now): inline under the Add Remote dialog's path field, and as the whole content of the on-host icon picker (`ui/remote_icon_picker.rs`). The icon picker is a dialog of its own because an inline list there is unusable — the Icon row sits partway down a scrolling preferences page with the Commands section under it, so the list gets only the sliver between the entry and the sheet's bottom edge, and growing the dialog buys a row or two and no more. Still gated off for remote: file watcher (both shells — the files live on the host), icon detection. Remote probing (config/detection/git) runs on worker threads via `util::worker::run` — never block the GTK main thread with ssh, and never poll a channel from `idle_add_local` (it busy-spins the main loop). Startup loads of unreachable remote projects notify once and retry in the background with capped backoff (`ProbeError::Unreachable` vs `Invalid` decides retryability)

## Config Files

- **Project config:** `tuxflow.toml` in project root (optional, version-controlled)
- **Global settings:** `~/.config/tuxflow/settings.toml` (appearance, keybindings, notifications)
- **Project state:** `~/.config/tuxflow/projects.toml` (open directories, custom commands, process order, names, icons)
- **Machine-local state:** `$XDG_STATE_HOME/tuxflow/state.toml` (window geometry, per-project `last_used` and sidebar `expanded`). Split out 2026-09-24 because the two files above get synced between machines (chezmoi): `last_used` moved on every start and the geometry is per-monitor, so every working day was a commit and two machines a conflict. Rule: anything that changes through ordinary USE rather than a deliberate edit belongs here, not in the synced files. The old keys (`[window]` in settings.toml, `[last_used]`/`[expanded]` in projects.toml) are still parsed (`legacy_*`, `skip_serializing`) to seed the file once when it is missing, and written out at once — the next save of either synced file drops its copy. An existing but unparseable state file falls back to defaults, never re-migrates

Every map serialized into these files is a `BTreeMap`, never a `HashMap` — `HashMap` iteration order is randomised per process, so each save rewrote the whole file in a fresh order and a one-key change surfaced as a whole-file diff (these files get tracked in dotfiles repos). `Vec` fields like `directories` and `process` keep user-defined order. `tests/projects_roundtrip_test.rs` pins the invariant.

## Formatting

Always run `cargo fmt --all` after changing Rust code, before committing.

## TODO

- **How the iced shell is built** — condensed from the migration's per-batch log (2026-08-26 → 2026-09-15, branch `iced-migration`, merged into main 2026-09-15). The full log, with every rejected alternative and every e2e transcript, is `git show 2c5c9df:CLAUDE.md` (line 124); the GTK app it was ported from is readable at `1db72a9:tuxflow-gtk/`. What follows is the part that still governs the code.
  - **Widget-tree shape is state.** iced keeps scroll offsets, focus and cursor in the tree, matched structurally, so a CONDITIONAL wrapper silently resets everything beneath it: `view()` always returns a `Stack` even with one layer (a `Column`→`Stack` swap at the root reset every scrollable when a menu opened); the DnD drop gap and the composer's chips row are always-present slots whose PADDING varies; a project move must reconcile `mouse_area` hover by tree position (`move_project` via `shown_projects`). Lay content out at FULL width inside a clipped container during the sidebar glide — laying out at the animated width re-wraps every card per frame.
  - **Modal = unfocus + chord gate.** Stack layers capture mouse but never keyboard (fork patch 19): raising menu/confirm/notice/update card calls `TerminalView::unfocus`, dismissal refocuses, and the app chords go dead while one is up (they act on the SELECTION — Alt+Shift+arrows under a delete card retargeted what Proceed deleted). Esc cascade order: notice → update card → confirm → menu → settings → palette → search → filter → forms. iced's `text_input` eats the first Esc to unfocus itself; do NOT route captured Esc through `listen_with` — it would misfire on a terminal's vim Esc.
  - **Chords.** App shortcuts are reserved in the terminal via `BindingAction::Passthrough` (`keys::reservations()`); Copy/Paste install the fork's OWN `Copy`/`Paste` at the user's chord (they never capture with nothing to do, so the app arm behind them — `explicit_remote_copy`/`paste_image` — runs off `AppAction::Copy`/`Paste`). `Hotkey` = `keyboard::listen()` = ignored-status keys only. Ctrl+Return is a built-in focus-terminal alias (`ctrl_held` at `PaletteSubmit`/`ComposerSend`). The `+` key is spelled `Plus`; no-modifier chords need `len > 1` (a captured bare `f` would eat every typed f); `chord_string` emits GTK's Ctrl,Shift,Alt order (string-compared conflict check); non-ASCII keys pass unexpanded. Known: rebinding leaves the OLD chord's Passthrough in open terminals until respawn.
  - **Terminal lifetime and runs.** A process's terminal lives as long as its ENTRY (GTK's one-VTE-per-process): `finalize_exit`/`stop` keep the grid (fork patch 16: `shutdown`/`respawn`/`feed`, `RESET_BETWEEN_RUNS` without RIS; `drain_on_exit` ON, or a short-lived command loses its whole output), a second run respawns into the same grid under `banner::run_separator`. `terminal.is_some()` is NOT "is running" — use `is_running()`; `term_id` names a terminal across runs, `run_id` a spawn (image paste, composer delivery and the restart/auto-open timers are keyed by it; an index would fire at whatever row moved into the slot). Fork patch 18 stamps events with a RUN GENERATION read at send time; main.rs drops mismatches (a child dying on the restart click otherwise parks its exit in the queue and the fresh run wears the crash). Patch 17 `clear()` mutates under the FairMutex the PTY loop takes, `ClearMode::All` BEFORE `Saved`. Patch 25 `recent_text` reads logs from the cursor up, history included, under the fair lock (never `sync`'s viewport). Never store grid coordinates across syncs.
  - **Keyboard fidelity (fork).** Patch 14: Alt+Enter → `\x1b\r`, unmatched Alt+text gets the ESC prefix (single-byte guarded), Ctrl+U = 0x15. Patch 20: named keys share `text_fallback` with the Character arm (a Shift-late space matched no binding and VANISHED — "spaces swallowed in agent chat"); Ctrl+Space rows added. Binding sweep: Ctrl+' no longer types SIGQUIT, Alt+Insert/Ctrl+F1-4 fixed. Patch 22 `SetHoldRelay` diverts bare-Space auto-repeats on remote AGENT terminals to core `remote/hold.rs` (a Python relay in a tmux CONTROL-MODE client sends `send-keys -l ' '` every 30 ms; ssh+tmux jitter had gaps >200 ms, which is Claude Code's release timer, so a held Space double-recorded). Patch 23/24: VTE's hollow unfocused cursor + `FocusMark` (`appearance.focus_indicator`, default `dim`; the dim is a constant 3.5 L* drop, `dim_alpha_for` derives alpha per scheme so Latte isn't a grey slab).
  - **Clipboard (X11 is the trap).** iced's X11 backend maps an owner's conversion refusal to `Ok("")`, so an image-only clipboard reads as `Some("")` (patch 15: empty Paste/PRIMARY = nothing to paste, no capture — what lets Ctrl+V fall through to `paste_image`); `text_editor`'s own Paste publishes NOTHING then, so the composer intercepts Ctrl+V and reads via `iced::clipboard::read()`. arboard serves a selection only while a handle lives → `STAGING_CLIPBOARD` static. Copy-on-select writes CLIPBOARD and PRIMARY (`Action::PublishSelection`); the remote tmux bridge is patch 21 (`ReportedSelectionGesture`: a drag crossing a CELL or a double/triple click, never a plain click) + core `ClipRoute`/`tmux_buffer_publishable` (age ≤ `SELECTION_MAX_AGE` on host clocks, hash differs, explicit Ctrl+Shift+C bypasses age) with `clip_seen` primed at open. Remote AGENT terminals rebind Ctrl+V to the widget's Paste (a raw ^V would have the agent read the HOST's clipboard); Ctrl+Shift+V / plain Ctrl+V with an image uploads to the host's shim slot (`upload_clipboard_image`) and sends ^V to agents, types the path to others.
  - **Sidebar.** Width = 25 % of window clamped 220–400; `clipped_label` (Fill + clip — iced text paints past its bounds). Category groups carry GTK's CAPTION ROW back (`section_caption`, 2026-09-22): the kind's glyph on the rows' dot centre line, small-caps title, hairline, running/total — in the gap's seat, hidden under a filter (a count over filtered rows counts the filter). Agents = in-house `tuxflow-agent-symbolic`, since `ai-brain-symbolic` exists in no Adwaita release and GTK's AGENTS caption was a broken image on stock systems. The gutter alternative (one glyph per group on the avatar's axis, rail to the last dot) is readable at `498732e` (the commit under the caption one). Two orthogonal card signals: accent RING + accent title = something running (`has_running()` counts Restarting and Reconnecting), corner gradient = active. Lifecycle glyphs reveal on hover via `mouse_area` + `hovered_row` (NOT `iced::widget::hover`), 140 ms `Anim` (generation-stamped 16 ms tick chain; reversing mid-glide restarts at `1 − eased` with the ease INVERTED); sidebar toggle glides 180 ms, rail swaps in only when the collapse lands. Working-agent signal = core `activity.rs` (ON needs a repaint burst ≥3 in 2 s, OFF 4 s quiet): ring breathes on every busy card, the light band rides the ACTIVE card only at peak 0.025 (0.10 "looked like it's going over the text"), 20 fps `Sweep` (30 fps cost ~25 % of a core under llvmpipe); `status_dot.rs` canvas arc rides the same phase (`TURNS_PER_PASS` whole, `SIZE` pinned at 10 px so rows never re-flow). `Linear::add_stop` overwrites out-of-order stops and drops past 8. Right-click menus mirror GTK's sections verbatim; destructive items behind GTK's confirmation texts; the cursor is tracked from `CursorMoved` because `on_right_press` carries no position. Recent-first = GTK's two tiers (running in START order ascending, stopped most-recent first, never-used in manual order), flips detected centrally at the top of `update()` on both edges. DnD (`dnd.rs` `DragArea` peeks at the event BEFORE delegating; GTK's 8 px threshold and exact drop rules; ghost offset by the grab point; edge auto-scroll tick chain).
  - **Status bar / git.** GTK's bar in order: remote glyph, `{project} r/t`, `Total r/t`, update chip; right: sync chip (`⎇ branch` ↓↑, click = fetch+ff-pull+push, spinner until `GitSyncSettled` — releasing on any `GitPolled` flashed pre-sync numbers), changes chip (`+N −M`, else file count, nothing when clean; text only), ↗ (icon-only, ahead of Focus), Focus, Clear, Stop (hidden when not running), Restart. Git colours are semantic constants (`GIT_ADDED` etc.), never `accent_for`. `poll_git_fetch` = GTK's report-twice (local numbers, then a fetch corrects), on every switch and every third 20 s tick (tick one fetches), guarded per project by `git_fetching` — a TIMESTAMPED map that expires after `FETCH_GUARD_EXPIRY`, and every git call goes through core's `output_within` (killed at `GIT_TIMEOUT`, fetch failures logged with stderr): a fetch that never reported back once left a project un-fetched for the whole session, ↓ never appearing for a teammate's push with nothing to read afterwards; `git_syncing` is a set keyed by project id. Git Changes is a full-pane view (`git_view.rs`): one `container(row![marker, rich_text])` per line (full-bleed bands need a per-line container; iced doesn't virtualize but caches shaped text), word-diff in core (`word_diff_ranges`: similarity by the SHORTER line, runs ALIGNED before pairing, whitespace dropped with `ALIGN_LOCALITY`), every async arrival generation-stamped AND path-keyed, `Msg::Done` gated on `busy == Some(action)`, selection held by PATH, view closed at the top of `update()` when the active project changes. Known: git C-quoted paths and renames show "(no diff available)".
  - **Settings.** `settings_ui.rs` = all GTK pages as a full-pane view, per-change save, live apply; every row has a consumer (pinned by `every_settings_row_reaches_the_matcher`). Colour scheme: `theme::Palette` behind `pal()` (dark/light, semantic hues with darkened light twins, `wash` ink black on light), `set_scheme` re-resolves remembered accent NAMES, "system" via the settings portal (`scheme.rs`, live). Terminal pane follows the TERMINAL scheme; full-pane views keep `BG_PANE`. Auto-hide sidebar = a press on the terminal pane (sensed by `DragArea`). Bold weight / letter spacing = fork patch 26 (spacing disables run merging). Default terminal → core `util/terminal_app.rs` (per-app exec flag pinned). Geometry: saved on close AND debounced ~1 s (cargo watch kills without a close), position from `window::position()` never `Moved`, `RestoreSettle`/`RestoreMaximize` kicks ~250–300 ms after launch (winit drops a pre-map maximize on X11), `RestoreMeasured` corrects only ≤100 px deltas; geometry lives in `self.state.window` (core `config::state`, NOT settings.toml).
  - **Remote (iced-specific on top of the shared note above).** Probe on a worker (`remote/probe.rs`; full detection, `apply_conservative_filter` on the worker, `detected_full` feeds Edit Project's Hidden pool); exit 255 = endless capped reconnect; reattach at startup; `scan_output_wrapped` badges; auto-tunnels with collision remap + `rewrite_clicked_url`; host-side `session_ports` poll (2 s → 30 s, `ensure_exact`); one-shot auto-open (badge-final + quiet period + port probe, or 5 s provisional grace, armed by user starts only). Mic bridge: `mic::set_enabled` before any host registers, boot WAITS on `wait_ready` (Claude Code caches its mic probe per process), `start()` re-registers the host on every remote run (the outage that has a process reconnecting killed the bridge too), `WindowClose` → `mic::shutdown()`. MCP: per-project `McpBridge::isolated()` + socket, snapshots rewritten at the top of `update()` on a fingerprint change, `TUXFLOW_MCP_SOCKET` in every child's env, remote half = `mcp/remote.rs` (`ssh -R` per project + Python `tuxflow-mcp` shim in `~/.local/bin` that CONNECTS to decide liveness). Notifications: `notify.rs` sends `Notify` over ONE lifelong zbus connection (Cinnamon/GNOME destroy a source when its sender's bus name vanishes — notify-rust's per-call connection made every banner die in ~3 ms) with the `desktop-entry` hint; the agent bell = `AEvent::Bell` → `notify_agent_idle` behind `notification_allowed` (window focus + terminal visible), silence fallback on `ActivityTick`, per-agent sounds; Claude Code rings only with `preferredNotifChannel = terminal_bell`, 60 s after "done" by default.
  - **Forms.** Add project (`add_project.rs`, Choose → Locate → Configure, no native picker: `list_local_dirs` is a deliberately identical twin of the remote listing, completions generation-stamped with App-level epochs strided 2^32 per open, debounced 250 ms only when remote; `needs_command_selection`/`conservative_names` live in core so the rule cannot drift; the selection step shows for ANY detection without a tuxflow.toml and opens with nothing selected — deselected rows are recorded as deleted, so skipping the step would hide everything), add command/agent (`AGENT_PRESETS` list, preset DERIVED from the command, `unique_agent_name` `claude` → `claude-2`; browser flag and watch patterns hidden for agents), add SSH (`config/ssh.rs` field rules), edit project (`toggle_entries` dedupes by COMMAND LINE — a rename onto a detected name must not swallow the script; deletions still recorded by name; enabled rows get the next free name), process edit (rename = delete old + custom copy + restart only when name/command/dir changed; `persist_process_order`). Forms are mutually exclusive, in the Esc cascade, and close centrally when the active project stops matching. Duplicate names are refused WITH the form kept and the reason on it.
  - **Composer** (agent terminals, `tools.agent_composer`): `text_editor`, Enter sends / Shift+Enter newline / Ctrl+Enter focuses the terminal, grows to `max_height(110)`, font = `appearance.font_size`. Images pasted become chips (`composer_attach_image` on a worker: PNG to `/tmp/.tuxflow-img-<stamp>.png` locally or `upload_temp_image` on the host), accepted only while the (project, term) pair is still selected, dropped centrally when it moves. Send = GTK's `deliver_composed`: per attachment stage as the agent's clipboard (`stage_clipboard_image` / arboard `set_image`) + 0x16 + `COMPOSER_INGEST` 400 ms, then the text as ONE bracketed paste with a leading `\n`, then `\r` 60 ms later, every write via `write_to_run(project, term, run)`.
  - **Single instance** (`instance.rs`): `$XDG_RUNTIME_DIR/tuxflow-instance.sock`; keys normalized in `main()` before the claim; ECONNREFUSED = stale file, unlink + rebind; any other failure runs standalone; `InstanceOpen` activates or opens + `gain_focus`. The listener is accepted ASYNC (`tokio::net::UnixListener`): a blocking `accept` on `spawn_blocking` never returns and a dropped tokio runtime waits for started blocking tasks forever, so every window close left the process alive with its tunnels and terminals (fixed 2026-09-21) — never park an unbounded blocking call on the runtime. **Packaging**: the iced shell is the root package `tuxflow`, `application_id = "com.tuxflow.TuxFlow"` (WM_CLASS/app_id = desktop file basename), `%U` file:// URIs decoded by `normalize_key`, .deb `depends = "$auto"` = libc6 only (the binary links libc alone), `TUXFLOW_UI=filter|search|palette|edit|settings|ssh|update|drop` seeds a state for screenshots.
  - **Sandbox lessons** (headless e2e): `XDG_RUNTIME_DIR` and sandbox HOME must be SHORT (SUN_LEN, `ssh -R` spec); bare Xvfb needs `XSetInputFocus` (under a reparenting WM: the frame's CLIENT child + warp the pointer) or injected keys are dropped, and only the FIRST injecting process gets through under metacity; `xvfb-run` dies with its child (relaunch tests need a standalone `Xvfb :N`); zsh does not word-split `$VAR` command lines; `pgrep -f`/`pkill -f` match the calling script; fixture commands must be absolute paths (his zsh prepends nvm's bin, so a fixture `codex` was shadowed by the real one); a fixture that "never exits" is usually zsh's new-user wizard on a HOME without `.zshrc`; a fixture agent must `stty raw` or 0x16 is eaten as VLNEXT; isolate `XDG_CONFIG_HOME` AND `XDG_STATE_HOME` (md5-guard the real projects.toml) and remember the live cargo-watch app relaunches on every edit; a debug build misrepresents terminal latency.
- **File upload to remote terminals** — DONE for drops (2026-09-17): `window::Event::FileDropped` → `deliver_files` → core `remote::upload_file` (`/tmp/.tuxflow-drop-<stamp>/<own name>` — agents judge a document by its name) → `typed_path` typed into the run that was on the pane (rides `ImagePasted`'s run stamp); local projects get the path typed, as VTE did. Failures raise a notice. Ignored under a modal or a full-pane view. winit 0.30 delivers drops on X11 only — no Wayland DnD. Ctrl+Shift+V takes the same road for a file-manager copy ONLY when the clipboard offers no text (`paste_image` falls back to arboard's `file_list`); Nemo/Nautilus also offer the path as text, so there the widget's Paste wins and types the LOCAL path — open: a remote-terminal Paste that asks for `text/uri-list` first (fork patch)
- **Split terminal view** — One pane at a time today; side-by-side needs a resizable split in `view_main`
- **Composer inline images** — Chips deliver attachments first, then text. Nicer: thumbnails at the cursor, send interleaves text chunks with Ctrl+V per image so `[Image #N]` lands where it was placed. Needs upload-in-flight gating on send and identity-based (not order) image→path matching
- **E2E harness** (est. 2–3 days, agreed 2026-09-15) — Core is covered (config, detector, port detector, log buffer, remote fs, MCP over its socket) and the app's pure helpers have unit tests; the lifecycle, terminal and form paths are verified only by ad-hoc Xvfb scripts that die with the session. Plan: (1) the driver, ~½ day — a Rust integration test that boots a standalone `Xvfb :N`, launches the RELEASE binary with isolated XDG dirs (short `XDG_RUNTIME_DIR`), injects keys/clicks via XTest with `XSetInputFocus`, reads screenshots and fixture output; (2) scenarios, 1–2 days, each already run by hand once — start/stop/restart/crash-restart against fixture commands, kept output across runs, composer delivery (fixture agent under `stty raw`), single instance, file watcher, settings live-apply, git view + sync chip against a scratch repo with an origin; (3) the remote half, ~½ day — the user-mode sshd sandbox from the memory notes as a fixture: detach/reattach, reconnect, tunnels, MCP forward; (4) CI, hours — a separate job on ubuntu-24.04 (Xvfb + sshd both run there), polling on observable state rather than sleeps so it doesn't flake, never blocking the fmt/clippy/test job. First commit = driver + lifecycle + composer + single instance.
- **CI** — `.github/workflows/ci.yml` (fmt/clippy/test on push + PR) and `release.yml` (on `v*` tags: builds the .deb + tarball, publishes the GitHub Release, force-pushes the signed apt repo to gh-pages). `scripts/release.sh` only bumps, tags and pushes — the artifacts come from the tag, so they land a few minutes later; a release checked too early looks half-published when it is just mid-run
